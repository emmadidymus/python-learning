"""
Python does not have a random() function to make a random number,
but it has a built-in random module that can be imported and used to make a random number.
"""

import random

print(random.randrange(1, 1000))